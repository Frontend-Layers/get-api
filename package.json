{
  "name": "@frontend-layers/get-api",
  "version": "1.0.0",
  "description": "Get API is a simple project that allows you to fetch some data from various services.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "nodemon",
    "test": "mocha --reporter mochawesome --reporter-options reportDir=./test/report,reportFilename=index.html,quiet=true",
    "publish": "npm publish --access public"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "chai": "^5.1.2",
    "mocha": "^11.0.1",
    "mochawesome": "^7.1.3",
    "node-fetch": "^2.7.0"
  }
}
